services:
  web:
    build: .
    environment:
      SESSION_SECRET: ${SESSION_SECRET:-dev-docker-session-secret-change-me}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./static/records:/app/static/records
